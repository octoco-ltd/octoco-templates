import {ExampleRepository} from "../example-repository";

export class MongoExampleRepository implements ExampleRepository {
    /*
    The MongoDB implementation of the example repo. You will create different implementations of this repo for differenet DBs e.g: DynamoDBExampleRepo, FirestoreExampleRepo, PostgresExampleRepo etc
     */
}