import {ExampleRepository} from "../example-repository";

export class InMemoryExampleRepository implements ExampleRepository {
    /*
    The in memory version of the example repo which is used for testing
     */
}