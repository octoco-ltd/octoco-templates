import {ExampleClient} from "../example-client";

export class HttpExampleClient implements ExampleClient {
    /*
     This is the actual implementation of the ExampleClient and will make the HTTP requests to the 3rd party
     */
}