import {ExampleClient} from "../example-client";

export class InMemoryExampleClient implements ExampleClient {
    /*
    The in-memory version of the example client which is used for testing
     */
}